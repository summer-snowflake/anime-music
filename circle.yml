machine:
  timezone:
    Asia/Tokyo
  ruby:
    version: 2.4.0
  node:
    version: 7.5.0

general:
  artifacts:
    - "~/anime-music/tmp/capybara/"

test:
  pre:
    - bundle exec rubocop app/ spec/ lib/
  post:
    - bundle exec rake brakeman
